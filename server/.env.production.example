# Production environment variables for server deployment
# Copy this to your deployment service (Railway, Render, etc.)

# Server Configuration
NODE_ENV=production
PORT=5000

# MongoDB Connection (Update with your MongoDB Atlas connection string)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/<dbname>?retryWrites=true&w=majority

# JWT Secret (Generate a strong random string)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d

# Email Configuration (Gmail or other SMTP service)
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password
EMAIL_FROM=BlockQuest <noreply@blockquest.com>

# Frontend URL (Update after deploying frontend)
CLIENT_URL=https://your-app.vercel.app

# CORS Configuration
ALLOWED_ORIGINS=https://your-app.vercel.app,http://localhost:5173

# Registration Cleanup Feature
REGISTRATION_CLEANUP_ENABLED=true

# Optional: Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
